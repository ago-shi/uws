---
defaultFileSet:
  src: "group_files/u6s-masters"
  owner: "root"
  group: "root"
  mode: '0644'

cloudInit:
  file: "cloud-init.disabled"
  path: "/etc/cloud/"

etcHosts:
  file: "hosts"
  src: "group_files/u6s-masters/"
  dest: "/etc/"
  owner: "root"
  group: "root"
  mode: '0644'

netplan:
  - file: "01-netcfg.yaml"
    src: "group_files/u6s-masters/netplan"
    dest: "/etc/netplan"
    mode: '0600'

netplanBuiltin:
  - file: "50-cloud-init.yaml"
    dest: "/etc/netplan"

modules_loadd:
  - file: "k8s.conf"
    src: "group_files/u6s-masters/modules_"
    dest: "/etc/modules-load.d/"
    owner: "root"
    group: "root"
    mode: '0644'

sysctld:
  - file: "k8s.conf"
    src: "group_files/u6s-masters/sysctl_"
    dest: "/etc/sysctl.d/"
    owner: "root"
    group: "root"
    mode: '0644'

fstabFile:
  file: "fstab"
  src: "group_files/u6s-masters/"
  dest: "/etc/"
  owner: "root"
  group: "root"
  mode: '0644'

k8sFile:
  kubeVIP:
    file: "kube-vip.yaml"
    src: "group_files/u6s-masters/"
    dest: "/etc/kubernetes/manifests/"
    owner: "root"
    group: "root"
    mode: '0644'
  dummyHosts:
    src: "group_files/u6s-masters/k8sDummyHosts.j2"
    dest: "/etc/hosts"
  adminConf:
    src: null # 自動生成
    dest: "/etc/kubernetes/admin.conf"
    mode: '0600'
  clusterJoin:
    file: "clusterJoin.info"
    src: null # 自動生成
    dest: "/etc/kubernetes/"
    mode: '0600'
  kubectlAuth:
    file: "config"
    src: "/etc/kubernetes/admin.conf"
    dest: "/home/kubeope/.kube"
    owner: kubeope
    group: kubeope
    mode: '0775'

# chronyService:
# -> group_vars/u6sMasters/systemd.yml

# containerdService:
# -> group_vars/u6sMasters/systemd.yml
